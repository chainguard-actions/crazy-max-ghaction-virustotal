name: Test empty files input
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Run action with empty files input (expect failure)
        id: action
        uses: .
        with:
          vt_api_key: fake-api-key-for-empty-test
          files: ''
          update_release_body: 'false'
        continue-on-error: true

      - name: Assert action failed due to empty files
        env:
          OUTCOME: ${{ steps.action.outcome }}
        run: |
          echo "outcome: $OUTCOME"
          if [ "$OUTCOME" != "failure" ]; then
            echo "FAIL: expected action to fail when files input is empty, but got outcome=$OUTCOME"
            exit 1
          fi
          echo "PASS: action correctly failed when files input is empty"
